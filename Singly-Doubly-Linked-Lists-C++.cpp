#include <iostream>
#include "clsLinkedList.h"
#include "clsDblLinkedList.h"

using namespace std;

int main() {

    cout << "=== Demo: Singly Linked List ===" << endl;

    // ????? ??????? ???? ????????
    clsLinkedList<int> sList;

    // ????? ????? ?? ???????
    cout << "\nInsertAtBeginning: ";
    sList.InsertAtBeginning(10);
    sList.InsertAtBeginning(20);
    sList.InsertAtBeginning(30);
    sList.Print(); // ???????: 30 20 10

    // ????? ???? ?? ???????
    cout << "\nInsertAtEnd: ";
    sList.InsertAtEnd(40);
    sList.Print(); // ???????: 30 20 10 40

    // ????? ?? ????
    cout << "\nFind 20: ";
    auto node = sList.Find(20);
    if (node != nullptr) cout << node->Value() << " found";
    else cout << "Not found";

    // ????? ??? ???? ?????
    cout << "\nInsertAfter 20: ";
    sList.InsertAfter(node, 25);
    sList.Print(); // ???????: 30 20 25 10 40

    // ??? ???? ????
    cout << "\nDeleteNode 10: ";
    sList.DeleteNode(10);
    sList.Print(); // ???????: 30 20 25 40

    // ??? ??? ????
    cout << "\nDeleteFirstNode: ";
    sList.DeleteFirstNode();
    sList.Print(); // ???????: 20 25 40

    // ??? ??? ????
    cout << "\nDeleteLastNode: ";
    sList.DeleteLastNode();
    sList.Print(); // ???????: 20 25

    // ?????? ?? ?????
    cout << "\nSize: " << sList.Size() << endl;

    // ?????? ??? ??????? ?????
    cout << "IsEmpty: " << (sList.IsEmpty() ? "Yes" : "No") << endl;

    // ?????? ??? ???? ?????
    cout << "IsExist 25: " << (sList.IsExist(25) ? "Yes" : "No") << endl;

    cout << "\n\n=== Demo: Doubly Linked List ===" << endl;

    // ????? ??????? ???? ????????
    clsDblLinkedList<int> dList;

    // ????? ????? ?? ???????
    cout << "\nInsertAtBeginning: ";
    dList.InsertAtBeginning(100);
    dList.InsertAtBeginning(200);
    dList.InsertAtBeginning(300);
    dList.PrintList(); // ???????: 300 200 100

    // ????? ???? ?? ???????
    cout << "\nInsertAtEnd: ";
    dList.InsertAtEnd(400);
    dList.PrintList(); // ???????: 300 200 100 400

    // ????? ??? ???? ????
    auto dNode = dList.Find(200);
    cout << "\nInsertAfter 200: ";
    if (dNode != nullptr)
        dList.InsertAfter(dNode, 250);
    dList.PrintList(); // ???????: 300 200 250 100 400

    // ??? ???? ????
    cout << "\nDeleteNode 100: ";
    auto delNode = dList.Find(100);
    if (delNode != nullptr)
        dList.DeleteNode(delNode);
    dList.PrintList(); // ???????: 300 200 250 400

    // ??? ??? ????
    cout << "\nDeleteFirstNode: ";
    dList.DeleteFirstNode();
    dList.PrintList(); // ???????: 200 250 400

    // ??? ??? ????
    cout << "\nDeleteLastNode: ";
    dList.DeleteLastNode();
    dList.PrintList(); // ???????: 200 250

    // ?????? ?? ?????
    cout << "\nSize: " << dList.Size() << endl;

    // ?????? ??? ??????? ?????
    cout << "IsEmpty: " << (dList.IsEmpty() ? "Yes" : "No") << endl;

    // ??? ???????
    cout << "Reverse: ";
    dList.Reverse();
    dList.PrintList(); // ???????: 250 200

    return 0;
}